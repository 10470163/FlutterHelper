# FlutterHelper demo

Sample Flutter project used with the FlutterHelper plugin.

```bash
cd examples/screenshot_demo
flutter pub get
```

Open this folder in IntelliJ IDEA / Android Studio with FlutterHelper installed.
